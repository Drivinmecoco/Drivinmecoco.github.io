//fast CHECK

/**
 * modifier.js is a module to modifies the current board
 * for testing
 * @namespace Modifier
 * @author Maxim Weill
 * @version 2023
 */
const Modifier = Object.create(null);


/**
 * Logs the entier board
 * @member Modifier
 * @function
 * @param {Checkers.Board} subBoard The board that will be inserted.
 * @param {Checkers.Board} board The board to insert into/overwrite.
 * @param {number[]} pos Top left coordinate
 */
Modifier.playerBoardInsert = function(subPlayerBoard,playerBoard,pos){

};


/**
 * Logs the entier board
 * @function
 * @param {Checkers.Board} subBoard The board that will be inserted.
 * @param {Checkers.Board} board The board to insert into/overwrite.
 * @param {number[]} pos Top left coordinate
 */
Modifier.boardInsert = function(subBoard,board,pos){

};